# This plugin is MIT licensed to match the switch license.
#
# Make switch easy to install and keep up to date if you're using a
# ZSH framework like Zgen or Antigen

export PATH=$(dirname $0):${PATH}
